#include<cstdio>
#include<iostream>
#include<cstring>
#include<algorithm>
#include<queue>
#include<vector>
#define maxn 100005
#define inf 0x3f3f3f3f
using namespace std;
int Ti[maxn];
int Ai[maxn];
int dp[maxn];
int main(){
	int T;
	scanf("%d",&T);
	while(T--){
		scanf("%d",&n);
		for(int i=0;i<n;i++){
			scnaf("%d",&Ti[maxn]);
		}
		for(int i=0;i<n;i++){
			scanf("%d",&Ai[maxn]);
		}
		memset(dp,inf,sizeof(dp));
		dp[0]=0;
		for(int i=0) 
	}
} 
