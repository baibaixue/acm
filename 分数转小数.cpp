#include<stdio.h>
#include<math.h>
int main(){
	long long int a,b;
	int c;
	int k=1;
	int sum;
	while(scanf("%lld%lld",&a,&b)!=EOF){
		if(a!=0&&b!=0&&c!=0){
			scanf("%d",&c);
			sum=a/b*pow(10.0,(double)c);
		printf("Case %d:\n",k);
		k++;)c));
		printf("%f",(double)sum/pow(10.0,(double
		}else
		break;
	}
	return 0;
}
